/**
 * Empty stub for Node.js built-ins (fs, path, etc.) when bundling for browser.
 * RDKit.js and similar WASM libraries reference these in Node-only code paths
 * that are never executed in the browser.
 */
module.exports = {};
