/**
 * Empty stub for Node.js built-ins (fs, path, etc.) when bundling for browser.
 * Some libraries reference these in Node-only code paths that are never
 * executed in the browser.
 */
module.exports = {};
